name: Aging
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pyqt=5.15.*
  - pip
  - pip:
      - pyqtgraph==0.14.0
      - PyOpenGL==3.1.10
      - PyOpenGL_accelerate==3.1.10
      - onnx  # 训练时 torch.onnx.export 导出 .onnx 所需
