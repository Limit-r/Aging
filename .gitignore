# ============================================================
# Python / PyQt5 / PyQtGraph
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
*.whl
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.venv/
venv/
env/
ENV/

# ============================================================
# 运行时产物
# ============================================================
logs/*.log
logs/*.log.*
*.log
console.log
*.tmp
*.swp
*.bak
*.orig
*.rej
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ============================================================
# IDE / 编辑器
# ============================================================
.vscode/
.idea/
*.iml
*.ipr
*.iws
*.swp
*~
.project
.classpath
.settings/
nbproject/
nbbuild/
nbdist/

# ============================================================
# Trae IDE（保留 .trae/documents/，排除 IDE 状态）
# ============================================================
.trae/skills/
.trae/agents/
.trae/cache/
.trae/logs/
.trae/state.json
.trae/index.json

# ============================================================
# Conda / Python 环境
# ============================================================
.conda/
conda-meta/

# ============================================================
# 模型 / 数据集 / 备份（不跟踪大文件）
# ============================================================
*.pth
*.onnx
*.onnx.data
*.pt
*.h5
*.pkl
*.npz
*.parquet
*.zip
*.tar
*.tar.gz
*.7z
*.mp4
*.avi
*.mov

# ============================================================
# 凭据 / 临时草稿
# ============================================================
.env
.env.local
*.key
*.pem
credentials.json
secrets.*
drafts/

# ============================================================
# 备份目录（v2 残留 / 外部备份）
# ============================================================
__legacy_v2__/
__archive__/

# 根级遗留数据集（已迁移至 ml/datasets/，禁止再跟踪）
/datasets/

# ============================================================
# 本地归档（历史权重 / 消融结果 / 生成输出，不入 Git）
# ============================================================
/archive/

# ============================================================
# 运行时生成配置（config_runtime.json 等一次性产物）
# ============================================================
config_runtime.json
*_runtime.json
eval_map_out/

# ============================================================
# 检测/推理输出（detect/outputs/ 一次性可视化结果）
# ============================================================
detect/outputs/

# ============================================================
# 训练模块生成数据（可由 gen_merged_txt.py / prepare_data_merged.py 再生）
# ============================================================
ml/datasets/merged/
ml/classifier/data_merged/
ml/ablation/outputs/

# 数据集原始图片与标注（位点图片 + 手工标注 XML，留本地并建议独立备份，不入 Git）
# 仅白名单保留：分集列表 / 类别表 / 包标记
ml/datasets/A/*
!ml/datasets/A/2025_*.txt
!ml/datasets/A/label*.txt
!ml/datasets/A/__init__.py
ml/datasets/FP/*
!ml/datasets/FP/2025_*.txt
!ml/datasets/FP/fp_51_frames.txt
!ml/datasets/FP/label.txt
!ml/datasets/FP/predefined_classes.txt
!ml/datasets/FP/__init__.py
# 分类器 ROI 裁剪样本（prepare_data_merged.py 可再生成）
ml/classifier/data_a/
ml/datasets/classifier_data/

# ============================================================
# 生成型训练日志（loss_* 曲线 / tfevents，可再生成）
# ============================================================
ml/weights/**/loss_*/
ml/train/weights/**/loss_*/

# ============================================================
# 临时提交信息（git commit -F 使用）
# ============================================================
commit_msg.txt

# ============================================================
# 持久化设置（密码/老化时长，不入库）
# ============================================================
data/settings.json

# ============================================================
# 临时诊断输出（根目录 · 2026-07-18 清理后补）
# ============================================================
# git diff 临时输出
/*_diff.txt
# 一次性诊断输出（根目录所有 .txt）
# 范围：仅根目录的 .txt（/* 前缀限定），不影响 .trae/documents/*.md
/*.txt
# 根目录一次性 PowerShell 脚本（编码修复等）
# 注：项目内 PowerShell 应放 scripts/ 目录
/*.ps1

# ============================================================
# 一次性诊断 / 临时测试脚本（tests/ 下 · 2026-07-18 清理后补）
# ============================================================
# 一次性诊断（用完即弃）
/tests/diag_*.py
# 临时 fix 验证（fix-1, fix-2, fix-13, fix-14, fix-15 ...）
/tests/test_fix*.py
# 一次性交互测试（手动 / qtest / 临时调试）
/tests/test_click_event.py
/tests/test_dblclick_debug.py
/tests/test_manual_dblclick.py
/tests/test_led_pos.py
/tests/test_qtest_click.py
# 根目录临时验证脚本（排查后清理）
/_onnx_check.py
/_qprocess_check.py
# 注：保留 tests/test_phase3_fix4.py / test_e2e_click.py /
#     test_pick_accuracy.py / test_mvp_compare.py 作为正式回归测试
